{% extends 'base.html.twig' %}

{% block title %}Nouvelle Adresse{% endblock %}

{% block body %}
    <div class="container my-5">
        <div class="card border-danger shadow-sm">
            <div class="card-body">
                <h1 class="card-title text-center mb-4 text-danger">Créer une Nouvelle Adresse</h1>

                {{ include('address/_form.html.twig') }}

                <div class="text-center mt-4">
                    <a href="{{ path('app_address_index') }}" class="btn btn-outline-secondary">Retour à la liste</a>
                </div>
            </div>
        </div>
    </div>
{% endblock %}
