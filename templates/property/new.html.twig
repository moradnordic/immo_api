{% extends 'base.html.twig' %}

{% block title %}Créer une nouvelle propriété{% endblock %}

{% block body %}
    <style>
        /* Make sure your CSS has enough specificity */
        .property-form-container .maison-field {
            background-color: #fff3cd !important;
            border-color: #ffeaa7 !important;
        }
    </style>
    <div class="container my-4">
        <h1 class="mb-4">Créer une nouvelle propriété</h1>

        <twig:PropertyFrom/>

        <div class="mt-4">
            <a href="{{ path('app_property_index') }}" class="btn btn-secondary">
                <i class="fas fa-arrow-left"></i> Retour à la liste
            </a>
        </div>
    </div>
{% endblock %}